<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@300..700&family=Rubik+Vinyl&display=swap" rel="stylesheet">
    <title>New password</title>
    <style>
        body{
            width: 100vw;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: "Fredoka", sans-serif;
        }
        form{
            display: flex;
            height: 200px;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            gap: 8px;
            background-color: rgb(34, 11, 104);
            padding: 80px;
            border-radius: 10px;
        }
        label{
            font-size: 20px;
            font-weight: 600;
            color: #fff;
        }
        input[type=password]{
            width: 250px;
            height: 30px;
            border-radius: 10px;
            border: 4px solid rgb(247, 64, 23);
            padding: 5px 10px;
        }
        input:focus,
        input:active {
            outline: none;
        }
        button{
            height: 40px;
            background-color: #fff;
            width: 50%;
            border-radius: 5px;
            border: none;
            font-family: "Fredoka", sans-serif;
            font-size: 15px;
            font-weight: 800;
            cursor: pointer;
        }
        @media only screen and (max-width: 600px) {
         form {
                padding: 40px;
            }
}
    </style>
</head>
<body>
    <form action="/user/password/reset/<%= token %>" method="post">
        <label for="newPassword">Password</label>
        <input type="password" id="newPassword" name="newPassword">
        <button>
            submit
        </button>
    </form>
</body>
</html>